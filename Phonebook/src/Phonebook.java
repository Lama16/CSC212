import java.util.Scanner;
public class Phonebook {
static Scanner input = new Scanner(System.in); 
LinkedList<Contact> contacts;	
	
public Phonebook() {
	
	
	
	
	
	
	
	
}







/*public boolean unique(Contact c) { //in linkedlist
	if(contacts.empty()) 
		return false;
	
	contacts.findfirst();
	while(true) {
		if(contacts.retrieve().name.equals(c.name)||contacts.retrieve().phonenumber.equals(c.phonenumber)) 
			break;
		contacts.findnext();
		if(!contacts.last())
			contacts.findnext();
			
	}
	return true;
	return false;
}
*/
}
